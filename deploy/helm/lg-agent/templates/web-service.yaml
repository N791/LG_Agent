apiVersion: v1
kind: Service
metadata:
  name: {{ include "lg-agent.fullname" . }}-web
  labels:
    app: {{ include "lg-agent.name" . }}-web
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "lg-agent.name" . }}-web
