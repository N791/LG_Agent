# 浏览器功能测试临时记录（2026-08-01）

## 测试范围

- trainee-web：`http://localhost:8081`（计划使用学员账号）
- web：`http://localhost:8080`（计划使用管理员账号）
- 验证依据：`Design_docs` 中的需求、核心业务流程、安全设计、API 设计及现有 Task Breakdown

## 执行状态

**BLOCKED — 尚未进入站点，未产生任何产品功能缺陷结论。**

按请求使用 Codex 内置浏览器插件连接时，浏览器控制进程连续启动失败。底层 Node 运行时在解析当前用户目录时触发：

```text
EPERM: operation not permitted, lstat 'C:\Users\ADMINI~1\AppData'
```

该错误发生在打开 `localhost`、登录或执行任何页面操作之前，因此不能把它归类为 trainee-web 或 web 的项目缺陷。

## 未执行项

- 两个站点的可达性、登录、退出及会话保持
- trainee-web 的任务大厅、工作区、文件编辑与保存、运行、提交、AI 导师、学习进度等流程
- web 的 Dashboard、组织/用户/角色、课程/任务管理、分析、AI 配置、权限与安全边界等流程
- 双端错误态、空态、刷新/返回、权限越权、跨端数据一致性和回归检查

## 后续动作

1. 恢复内置浏览器控制进程对当前用户运行目录的正常访问，或重启 Codex 后重新连接内置浏览器。
2. 重新执行完整的设计对照测试，并在本文件中逐条记录：编号、端、前置条件、操作步骤、期望、实际、严重度、证据和建议边界。
3. 只有在缺陷经过复现后，才据此向 `Design_docs/Design/10_3._可执行任务清单（Task_Breakdown）.md` 追加修复 Sprint。
