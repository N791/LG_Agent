name: Release Pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write
  packages: write

jobs:
  release:
    name: Release & Publish
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: Create Release Pull Request or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          publish: pnpm run release
          commit: "chore(release): version packages"
          title: "chore(release): version packages"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Images
        if: steps.changesets.outputs.published == 'true'
        run: |
          echo "Images would be built and pushed here."
          # Registry Agnostic build steps
          # export IMAGE_REGISTRY=${{ vars.IMAGE_REGISTRY || 'ghcr.io' }}
          # export IMAGE_NAMESPACE=${{ vars.IMAGE_NAMESPACE || github.repository_owner }}
          # docker build -t $IMAGE_REGISTRY/$IMAGE_NAMESPACE/lg-agent-api:latest -f packages/api/Dockerfile .
          # docker push $IMAGE_REGISTRY/$IMAGE_NAMESPACE/lg-agent-api:latest
          # (Add corresponding steps for vX.Y.Z and Git SHA tags)
